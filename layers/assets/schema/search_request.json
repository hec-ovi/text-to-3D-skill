{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "assets/search_request.json",
  "title": "AssetSearchRequest",
  "description": "Params in for a catalogue search.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "contractVersion": { "type": "string", "const": "1.0" },
    "query": { "type": "string", "description": "Words matched against the name, tags and categories. Every word must appear somewhere. Empty returns the whole catalogue, smallest first." },
    "limit": { "type": "integer", "minimum": 1, "maximum": 200, "default": 20 },
    "endpoint": { "type": "string", "default": "https://api.polyhaven.com", "description": "Poly Haven's API base. Overridable so the tests can run without network." },
    "timeoutSeconds": { "type": "integer", "minimum": 5, "maximum": 600, "default": 60 }
  }
}
