# local idea notes (kept out of the published repo)
docs/IDEA.md

# build artifacts
build/
# ...except the vendored three.js build, which the page actually loads
!layers/preview/web/vendor/three/build/
dist/
*.o
*.a
*.so
*.spv
compile_commands.json

# runtime data
out/
tmp/
*.glb
*.png
*.jpg
*.webp
!docs/assets/*.png
!layers/image2mesh/fixtures/*.png
!docs/assets/*.webp

# model weights (live in /home/hec/models, never in-tree)
*.gguf
*.safetensors
models/

# env / secrets
.env
.venv/
__pycache__/
node_modules/

# bench artifacts (results/*.json are committed, work/ is not)
layers/image2mesh/bench/work/

# preview layer test deps (the page itself needs no install)
layers/preview/node_modules/
layers/preview/package-lock.json
