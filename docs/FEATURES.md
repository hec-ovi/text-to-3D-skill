**Curling Particles (Franky Hung)**  
Cool Three.js particle effect using curl noise, adapted from a Three.js Journey lesson.  
Demo → https://projects.arkon.digital/threejs/ml-sharp-threejs/

**ThreeJS Assets**  
Web-optimized 3D asset packs for Three.js + free browser Studio to edit/animate them.  
Official → https://threejsassets.com/



# Ideas related:
- Allow asset search on the tool and preview using threejs assets.
- Particle is there for something else ignore
- check mp4 lowpoly the idea is we can lower poly and this is deeply important request:
    - How can we add character rigs for animatons in our generated models? this might require a deep research, so like adding the rig of legs, arms, and standard animations for games, etc.
- Simplify the toolkit, more lightweight, and intuitive, if possible transform it into an MCP, that connects to an API that exposes generate image, generate model, get result preview, download glb.
- Improve the disgusting preview overall, make an excellent side panel scroll, image and model preview as well
- Improve performance overall in all layers, sequence, etc
- Improve the skill overall make it with gbrain resolver/fatskill approach where we have an index of all the skills (generate the model, do the rig we will implement for a character, preview a generated model, etc) do it with ids and stuff so each semi tool returns an id or something the model can then provide a direct link of image preview or the URL of the preview with somethging like preview?modelid=modelID, for this research about gbrain resolver fat skill structure.